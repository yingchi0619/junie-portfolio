// Generated by npm run build. Edit miniprogram source modules.
(function(){const modules={"data/styleProfiles":function(module,exports,require){
const styles = {
  Minimal: ['Cotton tee', 'Straight-leg trousers', 'Clean sneakers'],
  Korean: ['Relaxed shirt', 'Wide-leg trousers', 'Canvas sneakers'],
  Sporty: ['Performance tee', 'Track pants', 'Running shoes'],
  Soft: ['Soft cotton blouse', 'Flowing trousers', 'Ballet flats'],
  Vintage: ['Ribbed tee', 'Straight-leg jeans', 'Retro sneakers'],
  Y2K: ['Fitted tee', 'Cargo trousers', 'Platform sneakers'],
  Streetwear: ['Oversized tee', 'Cargo trousers', 'High-top sneakers'],
  Office: ['Poplin shirt', 'Tailored trousers', 'Loafers'],
  Casual: ['Cotton tee', 'Relaxed jeans', 'Canvas sneakers'],
  Designer: ['Sculptural cotton top', 'Pleated trousers', 'Leather sneakers'],
  'Smart Casual': ['Knit polo', 'Chinos', 'Loafers'],
  Workwear: ['Utility shirt', 'Canvas trousers', 'Work boots'],
  Outdoor: ['Technical tee', 'Hiking trousers', 'Trail shoes'],
  Classic: ['Oxford shirt', 'Straight-leg chinos', 'Loafers'],
  Preppy: ['Pique polo', 'Pleated chinos', 'Boat shoes'],
  'Business Casual': ['Poplin shirt', 'Tailored chinos', 'Derby shoes']
};
const profiles = {
  Unrestricted: Object.keys(styles),
  Feminine: [
    'Minimal',
    'Korean',
    'Sporty',
    'Soft',
    'Vintage',
    'Y2K',
    'Streetwear',
    'Office',
    'Casual',
    'Designer'
  ],
  Masculine: [
    'Minimal',
    'Smart Casual',
    'Sporty',
    'Streetwear',
    'Korean',
    'Workwear',
    'Outdoor',
    'Classic',
    'Preppy',
    'Business Casual'
  ]
};
const palettes = {
  Neutral: ['#c1b49b', '#faf5e9', '#49564d'],
  Black: ['#424548', '#e6e5e2', '#252a2b'],
  'Earth tones': ['#9b785e', '#ece3cc', '#63664e'],
  Pastel: ['#c3b6ca', '#f5e5df', '#8e9eac'],
  Colorful: ['#b86542', '#ead49c', '#4e7682']
};
const defaults = {
  cityId: 'new-york',
  profile: 'Unrestricted',
  styles: ['Minimal'],
  color: 'Neutral',
  sensitivity: 'Normal',
  unit: 'F',
  onboarded: false
};
function normalizePreferences(value) {
  const p = Object.assign({}, defaults, value && typeof value === 'object' ? value : {});
  if (!profiles[p.profile]) p.profile = defaults.profile;
  p.styles = Array.isArray(p.styles)
    ? [...new Set(p.styles)].filter((s) => profiles[p.profile].includes(s)).slice(0, 3)
    : [];
  if (!p.styles.length) p.styles = ['Minimal'];
  if (!palettes[p.color]) p.color = 'Neutral';
  if (!['I get cold easily', 'Normal', 'I run warm'].includes(p.sensitivity))
    p.sensitivity = 'Normal';
  if (!['F', 'C'].includes(p.unit)) p.unit = 'F';
  p.onboarded = p.onboarded === true;
  return p;
}
module.exports = { styles, profiles, palettes, defaults, normalizePreferences };

},
"data/mockWeather":function(module,exports,require){
// Synthetic scenarios, never presented as observed weather. All engine values are SI.
const cities = [
  {
    id: 'new-york',
    name: 'New York',
    country: 'United States',
    latitude: 40.7128,
    longitude: -74.006,
    timezone: 'America/New_York',
    base: 22
  },
  {
    id: 'shanghai',
    name: 'Shanghai',
    country: 'China',
    latitude: 31.2304,
    longitude: 121.4737,
    timezone: 'Asia/Shanghai',
    base: 27
  },
  {
    id: 'tokyo',
    name: 'Tokyo',
    country: 'Japan',
    latitude: 35.6762,
    longitude: 139.6503,
    timezone: 'Asia/Tokyo',
    base: 24
  },
  {
    id: 'london',
    name: 'London',
    country: 'United Kingdom',
    latitude: 51.5072,
    longitude: -0.1276,
    timezone: 'Europe/London',
    base: 17
  },
  {
    id: 'los-angeles',
    name: 'Los Angeles',
    country: 'United States',
    latitude: 34.0522,
    longitude: -118.2437,
    timezone: 'America/Los_Angeles',
    base: 26
  }
];
const conditions = {
  sunny: 'Sunny',
  cloudy: 'Partly cloudy',
  rain: 'Rain',
  snow: 'Snow',
  night: 'Clear night',
  fog: 'Fog'
};
function getMockWeather(cityId = 'new-york', scenario = 'cloudy') {
  const city = cities.find((c) => c.id === cityId) || cities[0];
  const condition = conditions[scenario] ? scenario : 'cloudy';
  const temperature =
    condition === 'snow'
      ? -2
      : condition === 'rain'
        ? city.base - 5
        : condition === 'night'
          ? city.base - 6
          : city.base;
  const rainProbability = condition === 'rain' ? 85 : condition === 'snow' ? 65 : 15;
  const current = {
    temperature,
    feelsLike: temperature - 1,
    condition,
    rainProbability,
    wind: condition === 'rain' ? 28 : 12,
    humidity: condition === 'rain' ? 88 : 56,
    uv: condition === 'night' ? 0 : condition === 'sunny' ? 7 : 4,
    hour: condition === 'night' ? 22 : 10
  };
  const hourly = Array.from({ length: 24 }, (_, i) => {
    const hour = (current.hour + i) % 24;
    const temp = Math.round(temperature + 4 * Math.sin(((hour - 10) * Math.PI) / 12));
    const rain = condition === 'rain' || (condition === 'cloudy' && hour >= 18 && hour <= 21);
    return {
      hour,
      label: i === 0 ? 'Now' : String(hour).padStart(2, '0') + ':00',
      temperature: temp,
      rainProbability: rain ? 85 : rainProbability,
      condition: rain ? 'rain' : hour >= 20 || hour < 6 ? 'night' : condition
    };
  });
  const daily = Array.from({ length: 7 }, (_, i) => ({
    label: i === 0 ? 'Today' : ['Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'][i - 1],
    low: temperature - 6 + (i % 3),
    high: temperature + 4 - (i % 3),
    rainProbability: i === 2 ? 80 : rainProbability,
    condition: i === 2 ? 'rain' : condition
  }));
  return {
    city,
    current,
    hourly,
    daily,
    source: 'demo',
    updatedAt: '2026-09-14T14:00:00Z',
    dateLabel: 'Monday, September 14 · Demo',
    notice: 'Using demo weather data.'
  };
}
function searchCities(query = '') {
  const q = query.trim().toLowerCase();
  return cities.filter((c) => (c.name + ' ' + c.country).toLowerCase().includes(q));
}
function nearestCity(latitude, longitude) {
  const ranked = cities
    .map((c) => ({
      city: c,
      distance: Math.hypot(
        (c.latitude - latitude) * 111,
        (c.longitude - longitude) * 111 * Math.cos((latitude * Math.PI) / 180)
      )
    }))
    .sort((a, b) => a.distance - b.distance);
  return ranked[0].distance <= 100 ? ranked[0].city : null;
}
module.exports = { cities, conditions, getMockWeather, searchCities, nearestCity };

},
"utils/outfitEngine":function(module,exports,require){
const { styles, normalizePreferences } = require('../data/styleProfiles');
function getTemperatureLayer(feelsLike, sensitivity = 'Normal') {
  const adjusted =
    feelsLike + (sensitivity === 'I get cold easily' ? -3 : sensitivity === 'I run warm' ? 3 : 0);
  return {
    adjusted,
    level: adjusted < 5 ? 'winter' : adjusted < 15 ? 'warm' : adjusted < 23 ? 'light' : 'airy'
  };
}
function getRainLayer(weather) {
  return weather.condition === 'rain' || weather.rainProbability >= 50;
}
function getWindAdjustment(weather) {
  return weather.wind >= 25 ? -2 : 0;
}
function getStyleProfile(preferences, variant) {
  return preferences.styles[variant % preferences.styles.length];
}
function generateOutfitRecommendation(weather, preferences, hourly = [], variant = 0) {
  if (
    !weather ||
    !['temperature', 'feelsLike', 'rainProbability', 'wind', 'humidity', 'uv', 'hour'].every((k) =>
      Number.isFinite(weather[k])
    )
  )
    throw new Error('Weather is incomplete.');
  const p = normalizePreferences(preferences);
  const v = Math.abs(Math.floor(variant)) || 0;
  const style = getStyleProfile(p, v);
  const wardrobe = styles[style];
  const nextHours = hourly.slice(0, 12);
  const wet = getRainLayer(weather) || nextHours.some((h) => h.rainProbability >= 50);
  const wind = getWindAdjustment(weather);
  const layer = getTemperatureLayer(weather.feelsLike + wind, p.sensitivity);
  const swing = nextHours.length
    ? Math.max(...nextHours.map((h) => h.temperature)) -
      Math.min(...nextHours.map((h) => h.temperature))
    : 0;
  const winter = layer.level === 'winter';
  const cycle = Math.floor(v / p.styles.length) % 3;
  let outerwear = {
    winter: ['Insulated parka', 'Wool coat', 'Quilted winter coat'],
    warm: ['Wool overshirt', 'Quilted jacket', 'Knit cardigan'],
    light: ['Light jacket', 'Cotton overshirt', 'Unstructured blazer'],
    airy: [null, null, null]
  }[layer.level][cycle];
  if (wet)
    outerwear = winter
      ? 'Waterproof insulated coat'
      : layer.level === 'warm'
        ? 'Lined waterproof jacket'
        : 'Light waterproof shell';
  else if (wind && !winter) outerwear = 'Wind-resistant jacket';
  else if (swing >= 8 && !outerwear) outerwear = 'Packable overshirt';
  const top = winter
    ? 'Thermal base + ' + wardrobe[0].toLowerCase()
    : layer.level === 'warm'
      ? 'Long-sleeve ' + wardrobe[0].toLowerCase()
      : cycle === 1
        ? 'Breathable ' + wardrobe[0].toLowerCase()
        : cycle === 2
          ? 'Textured ' + wardrobe[0].toLowerCase()
          : wardrobe[0];
  const bottom = winter ? 'Lined ' + wardrobe[1].toLowerCase() : wardrobe[1];
  const shoes = winter ? 'Insulated grip boots' : wet ? 'Waterproof walking shoes' : wardrobe[2];
  const accessories = [];
  const reasons = [];
  reasons.push(
    layer.level === 'airy'
      ? 'Warm conditions favor light, breathable fabrics.'
      : winter
        ? 'Cold air calls for insulation and a thermal base.'
        : 'A removable layer balances comfort as temperatures change.'
  );
  if (wet) {
    accessories.push(weather.wind >= 35 ? 'Waterproof hood' : 'Compact umbrella');
    reasons.push('Rain is likely in the next 12 hours — keep a waterproof layer close.');
  }
  if (Math.abs(weather.temperature - weather.feelsLike) >= 3)
    reasons.push(
      'The air feels different from the thermometer; layers follow the feels-like temperature.'
    );
  if (wind) reasons.push('Strong wind adds chill; a wind-blocking outer layer helps.');
  if (swing >= 8) reasons.push('A large temperature swing makes removable layers useful.');
  if (weather.humidity >= 75)
    reasons.push('High humidity favors quick-drying, breathable fabrics.');
  if (weather.uv >= 6 && weather.hour >= 6 && weather.hour < 20) {
    accessories.push('Sun hat', 'Sunglasses');
    reasons.push('Elevated UV: add shade and sun protection.');
  }
  if (weather.hour >= 20 || weather.hour < 6) {
    accessories.push('Reflective detail');
    reasons.push('After dark, a reflective detail improves visibility.');
  }
  if (winter) accessories.push('Warm socks', 'Scarf');
  if (p.sensitivity !== 'Normal')
    reasons.push(
      p.sensitivity === 'I get cold easily'
        ? 'Adjusted warmer because you get cold easily.'
        : 'Adjusted lighter because you run warm.'
    );
  const pieces = [outerwear, top, bottom, shoes].filter(Boolean);
  const id = [style, p.color, layer.level, ...pieces, ...accessories].join('|');
  return {
    id,
    outerwear,
    top,
    bottom,
    shoes,
    accessories,
    explanation: reasons.join(' '),
    reasons,
    style,
    color: p.color,
    layer: layer.level,
    pieces,
    title: [
      'A little ease. A lot of you.',
      'Your everyday, reimagined.',
      'Comfort, with character.'
    ][cycle],
    comfortScore: Math.max(65, 96 - (wet ? 4 : 0) - (wind ? 3 : 0) - (swing >= 8 ? 3 : 0)),
    scoreLabel: 'Rule fit · not a measured comfort score',
    illustration:
      '/assets/look-' +
      layer.level +
      '-' +
      Object.keys(require('../data/styleProfiles').palettes).indexOf(p.color) +
      '.png'
  };
}
module.exports = {
  getTemperatureLayer,
  getRainLayer,
  getWindAdjustment,
  getStyleProfile,
  generateOutfitRecommendation
};

},
"utils/storage":function(module,exports,require){
const { normalizePreferences } = require('../data/styleProfiles');
let lastError = '';
function read(key, fallback) {
  try {
    const raw =
      typeof wx !== 'undefined'
        ? wx.getStorageSync('mw:' + key)
        : localStorage.getItem('mw:' + key);
    return raw ? (typeof raw === 'string' ? JSON.parse(raw) : raw) : fallback;
  } catch (e) {
    lastError = 'Local storage is unavailable. Changes may not persist.';
    return fallback;
  }
}
function write(key, value) {
  try {
    if (typeof wx !== 'undefined') wx.setStorageSync('mw:' + key, value);
    else localStorage.setItem('mw:' + key, JSON.stringify(value));
    lastError = '';
    return true;
  } catch (e) {
    lastError = 'Unable to save on this device. Storage may be full.';
    return false;
  }
}
function getPreferences() {
  return normalizePreferences(read('preferences', {}));
}
function getSaved() {
  const saved = read('saved', []);
  return Array.isArray(saved)
    ? saved
        .filter(
          (o) =>
            o &&
            typeof o.id === 'string' &&
            Array.isArray(o.pieces) &&
            typeof o.explanation === 'string' &&
            typeof o.illustration === 'string'
        )
        .slice(0, 30)
    : [];
}
function toggleSaved(outfit) {
  const saved = getSaved();
  const exists = saved.some((o) => o.id === outfit.id);
  const next = exists
    ? saved.filter((o) => o.id !== outfit.id)
    : [Object.assign({}, outfit, { savedAt: new Date().toISOString() }), ...saved].slice(0, 30);
  return { ok: write('saved', next), saved: !exists };
}
module.exports = { read, write, getPreferences, getSaved, toggleSaved, getError: () => lastError };

},
"utils/weatherFormatter":function(module,exports,require){
const { conditions } = require('../data/mockWeather');
function temperature(c, unit = 'F') {
  return Math.round(unit === 'F' ? (c * 9) / 5 + 32 : c) + '°';
}
function formatWeather(data, unit) {
  const values = data.hourly.map((h) => h.temperature),
    min = Math.min(...values),
    max = Math.max(...values);
  const low = Math.min(...data.daily.map((d) => d.low)),
    high = Math.max(...data.daily.map((d) => d.high));
  return Object.assign({}, data, {
    temperature: temperature(data.current.temperature, unit),
    feelsLike: temperature(data.current.feelsLike, unit),
    conditionLabel: conditions[data.current.condition] || data.current.condition,
    metrics: [
      { label: 'Humidity', value: data.current.humidity + '%' },
      {
        label: 'Wind',
        value:
          Math.round(unit === 'F' ? data.current.wind / 1.609 : data.current.wind) +
          (unit === 'F' ? ' mph' : ' km/h')
      },
      { label: 'UV index', value: String(data.current.uv) },
      { label: 'Rain', value: data.current.rainProbability + '%' }
    ],
    hourly: data.hourly.map((h) =>
      Object.assign({}, h, {
        displayTemp: temperature(h.temperature, unit),
        height: 18 + ((h.temperature - min) / Math.max(1, max - min)) * 38,
        conditionLabel: conditions[h.condition]
      })
    ),
    daily: data.daily.map((d) =>
      Object.assign({}, d, {
        displayLow: temperature(d.low, unit),
        displayHigh: temperature(d.high, unit),
        left: (100 * (d.low - low)) / Math.max(1, high - low),
        width: (100 * (d.high - d.low)) / Math.max(1, high - low),
        conditionLabel: conditions[d.condition]
      })
    ),
    shift:
      max - min >= 8
        ? 'Large temperature swing today — layering recommended.'
        : data.hourly.some((h) => h.rainProbability >= 50)
          ? 'Rain ahead — keep a waterproof layer within reach.'
          : 'A steady forecast. Dress for comfort throughout the day.',
    periods: [
      { label: 'Morning', hour: 9 },
      { label: 'Afternoon', hour: 15 },
      { label: 'Evening', hour: 21 }
    ].map((p) => {
      const h = data.hourly.find((h) => h.hour === p.hour) || data.hourly[0];
      return {
        label: p.hour < data.current.hour ? 'Next ' + p.label.toLowerCase() : p.label,
        temperature: temperature(h.temperature, unit),
        rain: h.rainProbability
      };
    })
  });
}
module.exports = { temperature, formatWeather };

}},cache={};function resolve(from,target){const parts=(from.slice(0,from.lastIndexOf('/')+1)+target).split('/'),out=[];for(const part of parts){if(part==='..')out.pop();else if(part!=='.')out.push(part);}return out.join('/');}function load(id){if(cache[id])return cache[id].exports;const module={exports:{}};cache[id]=module;modules[id](module,module.exports,p=>load(resolve(id,p)));return module.exports;}window.MW={...load('data/styleProfiles'),...load('data/mockWeather'),...load('utils/outfitEngine'),...load('utils/weatherFormatter'),storage:load('utils/storage')};})();